--- ipa/tests/lib_tests.rs
+++ ipa/tests/lib_tests.rs
@@ -124,7 +124,5 @@

 #[test]
 fn test_global_combine_with_modifier() {
-    assert!(
-        ipa::combine_with_modifier("p", "ʰ").is_none()
-            || ipa::combine_with_modifier("p", "ʰ").is_some()
-    );
+    assert!(ipa::combine_with_modifier("p", "ʰ").is_some());
 }
